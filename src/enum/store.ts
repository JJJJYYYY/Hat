export enum TYPE {
  CHANGE_MODEL = 'changeModel',
  CHANGE_NOT_ACTIVE_MODEL = 'changeNotActiveModel',
  SINGLE = 'single',
  MULTIPLY = 'multiply',
  PRESS_MULTIPLY = 'press_ctrl',
  ADD_ELE = 'add_ele'
}
